/**
 * Plugin Templates
 *
 * These templates provide starting points for creating Audiio plugins.
 * Copy the relevant template to your project and implement the abstract methods.
 *
 * Available templates:
 * - metadata-provider.ts - For providers that fetch track/artist/album metadata
 * - stream-provider.ts - For providers that resolve playable audio streams
 * - lyrics-provider.ts - For providers that fetch song lyrics
 */

// Template exports (for reference only, do not use directly)
export { ExampleMetadataProvider } from './metadata-provider';
export { ExampleStreamProvider } from './stream-provider';
export { ExampleLyricsProvider } from './lyrics-provider';
