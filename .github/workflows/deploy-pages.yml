name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'packages/landing/**'
      - 'packages/mobile/src/web/**'
      - 'packages/icons/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build icons (shared dependency)
        run: npm run build --workspace=@audiio/icons

      - name: Build landing page
        run: npm run build --workspace=@audiio/landing
        env:
          NODE_ENV: production

      - name: Build mobile remote portal
        run: npm run build:web --workspace=@audiio/mobile
        env:
          NODE_ENV: production
          GITHUB_PAGES: 'true'

      - name: Combine builds for deployment
        run: |
          mkdir -p ./deploy
          # Landing page at root
          cp -r ./packages/landing/dist/* ./deploy/
          # Remote portal at /remote
          mkdir -p ./deploy/remote
          cp -r ./packages/mobile/dist/web/* ./deploy/remote/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './deploy'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
